ffb_follow_node:
  ros__parameters:

    # トピック名
    # kobuki用
    auto_cmd_topic: /cmd_vel_auto
    odom_topic: /odom
    manual_active_topic: /handle/manual_active

    # G923デバイス
    device_path: /dev/input/by-id/usb-Logitech_G923_Racing_Wheel_for_PlayStation_4_and_PC_USYMUGUXEREJOFORUFUMEZIDU-event-joystick

    # Autocenter
    auto_autocenter: 0
    manual_autocenter: 60
    idle_autocenter: 15
    shutdown_autocenter: 80

    # 角度変換
    handle_limit_deg: 450.0
    yaw_to_handle_ratio: 1.25
    invert_yaw_sign: true
    center_offset_deg: -3.0

    # 自律旋回判定
    auto_angular_threshold: 0.05
    auto_cmd_timeout: 0.3

    # 自律旋回中のSpring
    auto_spring_coeff: 15000       # 0x4900 
    auto_spring_saturation: 22000  # 0x6000

    # 手動操作中のSpring
    manual_spring_coeff: 13500 # 18688yatta
    manual_spring_saturation: 20000 # 24576

    # 待機中のSpring
    idle_spring_coeff: 13500       # 0x3000
    idle_spring_saturation: 20000  # 0x4800

    # Spring共通設定
    spring_deadband: 0
    spring_replay_ms: 30000

    # Spring更新
    max_center_step: 150 # 400yatta
    coeff_step: 512               # 0x0200
    saturation_step: 512          # 0x0200

    center_send_threshold: 5 # 20yatta
    coeff_send_threshold: 128     # 0x0080
    saturation_send_threshold: 128

    effect_refresh_sec: 0.5 # 1.0yatta
    spring_update_period: 0.01 # 0.05yatta
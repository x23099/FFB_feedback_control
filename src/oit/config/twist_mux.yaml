twist_mux:
  ros__parameters:
    topics:
      joystick:
        topic: "/cmd_vel_joy"
        timeout: 0.5
        priority: 100
      navigation:
        topic: "/cmd_vel_auto"
        timeout: 0.5
        priority: 10
